<p id="notice"><%= notice %></p>

<%= link_to 'back', project_categories_path %>
<h1>Tasks</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Deadline</th>
      <th>Complete</th>
      <th>Timelimit</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.name %></td>
        <td><%= date_time(task.deadline) %></td>
        <td><%= task.complete %></td>
        <td><%= task.timelimit %></td>
        <td><%= link_to 'Show', project_category_task_path(@project, @category, task) %></td>
        <td><%= link_to 'Edit', edit_project_category_task_path(@project, @category, task) %></td>
        <td><%= link_to 'Destroy', project_category_task_path(@project, @category, task), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Task', new_project_category_task_path %>
